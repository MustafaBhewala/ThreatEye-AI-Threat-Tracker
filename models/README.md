# ML Models Directory

This directory contains trained machine learning models:
- risk_classifier.pkl - Supervised classifier (Safe/Suspicious/Malicious)
- anomaly_detector.pkl - Unsupervised anomaly detection model

Models are retrained periodically based on new threat data.

Note: Model files are ignored by git due to size.
